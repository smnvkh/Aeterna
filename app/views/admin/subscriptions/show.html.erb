<p style="color: green"><%= notice %></p>

<%= render @subscription %>

<div>
  <%= link_to "Edit this subscription", edit_admin_subscription_path(@subscription) %> |
  <%= link_to "Back to subscriptions", admin_subscriptions_path %>
  <%= link_to "Destroy this subscription", [:admin, @subscription], data: { turbo_stream: false, turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
</div>
